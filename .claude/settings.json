{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "if": "Bash(git push:*)",
            "command": "if [ ! -f Makefile ]; then exit 0; fi; make check && INCLUDE_INTEGRATION=1 make test-integration || { echo '{\"continue\":false,\"reason\":\"Pre-push gate failed: make check or integration tests did not pass.\"}'; exit 0; }",
            "timeout": 600,
            "statusMessage": "Running pre-push gate (make check + integration tests)..."
          }
        ]
      }
    ]
  }
}
